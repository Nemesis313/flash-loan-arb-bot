// bridges.rs

pub fn bridge_to_l2(token: Token, amount: U256) -> Result<BridgeTx> {
  // Call L1 > L2 bridge 
}

pub fn bridge_to_l1(token: Token, amount: U256) -> Result<BridgeTx> {
  // Call L2 > L1 bridge
}
